import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function DashboardsPage() {
  return (
    <div>
      <Title level={3}>Dashboard Builder</Title>
      <Card>Dashboard builder coming soon</Card>
    </div>
  );
}