import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function InsertionAnalyticsPage() {
  return (
    <div>
      <Title level={3}>Insertion Analytics</Title>
      <Card>Insertion analytics dashboard coming soon</Card>
    </div>
  );
}